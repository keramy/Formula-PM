import React from 'react';
import LoginPage from '../components/auth/LoginPage';

// Re-export LoginPage as Login for consistency
const Login = () => {
  return <LoginPage />;
};

export default Login;